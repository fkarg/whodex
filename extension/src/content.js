// TODO P1h-3: implement LinkedIn profile scraping
// Runs on *://*.linkedin.com/in/* pages.
// Fields to extract: name (name.full), title (job.title), company (job.org), linkedin_url (linkedin.url)
// Sends extracted record to background.js via browser.runtime.sendMessage.

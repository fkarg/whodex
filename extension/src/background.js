// TODO P1h-3: implement background event page logic
// Receives messages from content.js and POSTs to the configured whodex /ingest endpoint.
// Payload envelope: { records: [{ source_kind: "linkedin_ext", name, title, company, linkedin_url }] }
